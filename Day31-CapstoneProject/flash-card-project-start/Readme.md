firstly do UI Setup


1. Create a window
2. add canvas to the window
3. add text to the canvas which is title and the word that needs to be translated
4. add buttons to the window which will be used to mark if the user got the answer right or wrong
5. add functionality to the buttons so that when the user clicks on them, the next word is shown on the canvas
6. add functionality to the canvas so that after a few seconds, the word is flipped and the translation is shown
7. add functionality to keep track of the words that the user got right and wrong, and to store it into the csv file 
so that the user can see their progress and review the words they got wrong later on.