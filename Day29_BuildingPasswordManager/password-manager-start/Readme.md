1.Insert the image of the locker
2.Create the labels and entries of each as required
3. Ftech the data then save the details entered to the file in specific format
4. clear everything as soon as written into the file 
5. Display the message box, to yes or no - if the user entered fields should be written in the file to save into it
6. if any fields are empty display error message
7. create function generate password by using list comprehension for numbers,symbols and letters,add them using "JOIN"
8. when user clicks on the Generate password button, password will get generate so pass it on to the button of generate password